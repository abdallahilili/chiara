import 'package:flutter/material.dart';

const backgroundColor = Color(0xFFF5F8E9); // Blanc
const textColor = Color.fromRGBO(0, 0, 0, 1);
const appBarColor = Color.fromRGBO(31, 44, 52, 1);
const webAppBarColor = Color.fromRGBO(42, 47, 50, 1);
const messageColor = Color(0xFF00943F); // Vert principal
const senderMessageColor = Color(0xFF009541); // Variation de vert
const tabColor = Color(0xFF009440); // Autre nuance de vert
const searchBarColor = Color(0xFF00913A); // Vert plus clair
const dividerColor = Color.fromRGBO(37, 45, 50, 1);
const chatBarMessage = Color.fromRGBO(30, 36, 40, 1);
const mobileChatBoxColor = Color.fromRGBO(31, 44, 52, 1);

const whiteColor = Colors.white;
const greenCustomColor = Color(0xFF5CB67A);
const greyColor = Colors.grey;
const blackColor = Colors.black;
const goldColor = Color(0xFFBFA54F); // Doré extrait de l'image
